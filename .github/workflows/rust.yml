name: Rust

on:
  repository_dispatch:
    types: [build]
  workflow_dispatch:
    inputs:
      name:
        description: 'Run?'
        required: true
        default: 'YES!'
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build-windows-msvc:
    name: Build Windows 64-bit (MSVC + Ninja)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: hecrj/setup-rust-action@master
        with:
          rust-version: stable

      - name: Install Ninja
        run: choco install ninja -y

      - name: Set CMake generator to Ninja
        run: echo "CMAKE_GENERATOR=Ninja" >> $env:GITHUB_ENV

      - name: Add MSVC target
        run: rustup target add x86_64-pc-windows-msvc

      - name: Build 64-bit MSVC target
        run: cargo +nightly build -Zbuild-std --release --target x86_64-pc-windows-msvc

      - uses: actions/upload-artifact@v4
        with:
          name: GM8Emulator-windows-msvc-x64
          path: target/x86_64-pc-windows-msvc/release/*.exe
          
  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install build dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y --no-install-recommends \
            git build-essential libasound2-dev libxcb-shape0-dev \
            libxcb-xfixes0-dev libgtk-3-dev libssl-dev libxcb-xinput-dev \
            libxcb-xkb-dev libxcb-cursor-dev default-jre-headless cmake g++ \
            libudev-dev curl wget libpulse-dev libjack-dev zip
          sudo apt-get clean
      - uses: actions/setup-node@v4
        with:
          node-version: '10.x'
      - uses: hecrj/setup-rust-action@master
        with:
          rust-version: stable
      - name: Rust set up
        run: |
          rustup self update
          rustup update
          git submodule update --init --recursive
      - name: Build Linux release
        run: |
          cargo build --release
          mkdir -p archive
          cp target/release/gm8emulator archive
          find . -maxdepth 3
      - uses: actions/upload-artifact@v4
        with:
          name: GM8Emulator-linux
          path: archive

  collect:
    name: Collect and package artifacts
    runs-on: ubuntu-latest
    needs: [build-windows-msvc, build-linux]
    steps:
      - uses: actions/checkout@v4
      - name: Download Windows MSVC x64 artifact
        uses: actions/download-artifact@v4
        with:
          name: GM8Emulator-windows-msvc-x64
          path: artifacts/windows-msvc-x64
      - name: Download Linux artifact
        uses: actions/download-artifact@v4
        with:
          name: GM8Emulator-linux
          path: artifacts/linux
      - name: Inspect downloaded artifacts
        run: |
          echo "Windows MSVC x64 files:"
          ls -la artifacts/windows-msvc-x64 || true
          echo "Linux files:"
          ls -la artifacts/linux || true
      - name: Create combined release zip
        run: |
          mkdir -p release
          cp artifacts/windows-msvc-x64/* release/ || true
          cp artifacts/linux/* release/ || true
          cd release
          zip -r ../GM8Emulator-all-platforms.zip . || true
          cd ..
      - name: Upload combined artifact
        uses: actions/upload-artifact@v4
        with:
          name: GM8Emulator-all
          path: GM8Emulator-all-platforms.zip
